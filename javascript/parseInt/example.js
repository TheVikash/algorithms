var month = "06", year = "09"; 

// to avoid inconsistency always use radix in parseInt
console.log(parseInt(month, 10) ); 
console.log(parseInt(year, 10) );


// other usefull patterns
console.log(+month);
console.log(Number(month));